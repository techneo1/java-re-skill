# ============================================================
# Test configuration  (test/resources/app.properties)
# Overrides main config during test runs.
# ============================================================
app.name=HR Test Environment
app.max.employees=50
app.default.salary=30000.00
app.audit.enabled=true
